# Toast — Playwright 测试说明

- 测试目标：验证吐司通知的出现/消失、堆栈顺序与自动消失逻辑（如实现）。
- 推荐测试用例：
  - 出现/消失：触发吐司并断言可见性，等待自动消失并断言不可见。
  - 堆栈顺序：连续触发多条吐司并截图，确认展示顺序与样式。
  - 手动关闭：点击关闭按钮并断言移除。
- 建议测试文件：`apps/website/tests/toast.spec.ts`
- 注意：在截图前等待定时器（或在示例页面提供可控定时的测试钩子）。
