import { vars } from "@idealjs/camphora-styled";
import { style } from "@vanilla-extract/css";

export const modal = style({
  marginTop: "8px",
  border: "0px",
});

export const modalCard = style({
  width: vars.size.card.medium,
});
